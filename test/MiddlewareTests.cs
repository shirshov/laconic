using Xunit;

namespace Laconic.Tests
{
    public class MiddlewareTests
    {
        [Fact]
        public void should_call_middleware()
        {
            // var binder = Binder.Create("state", (s, g) => s);
            // binder.UseMiddleware((state, signal, next) =>
            // {
            //     next();
            //     return state;
            // });
        }
    }
}